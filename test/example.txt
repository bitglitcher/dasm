



struct something
{
    byte 5, apple;
    byte 4, lime;
    byte 8, lol;
}

add ax, bx, cx;

sub ax, bx, cx;

add ax, bx, cx;
sub ax, bx, cx;
and ax, bx, cx;
xor ax, bx, cx;
or ax, bx, cx;
not ax, bx, cx;
shftl ax, bx, cx;
shftr ax, bx, cx;



load hi, 0xff;
