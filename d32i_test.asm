






main:
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;
    mov ax, 0x20;

